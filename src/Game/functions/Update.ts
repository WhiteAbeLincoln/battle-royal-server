
import { SocketFunc, bugger, Message } from '../index'
import { MessageKeys, EmitKeys } from '../keys'

export const sendUpdate: SocketFunc = (socket, io) => (auth, state) => {
//   socket.on(MessageKeys., (message: Message<string>) => {
//     bugger('update recieved from %s: %s', auth.token.gamertag, message.data)

//   })
}
